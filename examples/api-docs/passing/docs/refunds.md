# refunds

Read the refunds resource.

```
curl -s https://api.example.com/v2/refunds \
  -H "Authorization: Bearer $API_KEY"
```

Returns a paginated list. See `refunds.json` for the response schema.
