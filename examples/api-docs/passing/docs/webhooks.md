# webhooks

Read the webhooks resource.

```
curl -s https://api.example.com/v2/webhooks \
  -H "Authorization: Bearer $API_KEY"
```

Returns a paginated list. See `webhooks.json` for the response schema.
